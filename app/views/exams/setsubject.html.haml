%h2 Select subjects for this exam
= simple_form_for setsubject_path, method: :post do |f|
  - @exam.grade.subjects.each do |s|
    = check_box_tag "subjects[]", s.id
    = s.subject
  = submit_tag "Next"
