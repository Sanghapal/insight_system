- if current_user.roles == 'admin' && current_user.roles == 'trainer'
  %h1 New student
- else
  %h1 My profile

= render 'form'

= link_to '<button type="button">Cancel</button>'.html_safe, students_path
